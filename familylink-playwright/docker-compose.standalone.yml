version: '3.8'

services:
  familylink-auth:
    # Option 1: Build locally from Dockerfile.standalone
    build:
      context: .
      dockerfile: Dockerfile.standalone
    # Option 2: Use pre-built standalone image
    # image: ghcr.io/noiwid/familylink-auth:standalone
    container_name: familylink-auth
    ports:
      - "8099:8099"  # Web interface
      - "6080:6080"  # noVNC web interface
    volumes:
      - ./data:/share/familylink:rw
    # Increase shared memory size for Chromium (default 64MB is too small)
    shm_size: '2gb'
    environment:
      - LOG_LEVEL=info
      - AUTH_TIMEOUT=300
      - SESSION_DURATION=86400
      - VNC_PASSWORD=familylink
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8099/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
